title For Loop Demo

axes w=500 h=500 x=10 y=10 pan zoom drag resize
  # a row of circles using a for loop
  for i=[0, 400, 50]
    circle x=i y=100 r=6 fill=#44d6ff

  # a grid of rectangles using nested for loops
  for row=[0, 200, 50]
    for col=[0, 300, 50]
      rect x=col y=row w=40 h=40 fill=#3b82f6 stroke=#93c5fd

  # vertical lines with explicit step
  for x=[500, 900, 80]
    line x1=x y1=50 x2=x y2=350 stroke=#f59e0b width=2
